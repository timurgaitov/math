# Exercises

One file per exercise: `NNNN-<slug>.md`, numbered in the order given.

Each file contains: the problem statement, the unit it belongs to, the
student's attempt or final solution, and a status line:
`status: open | solved | revisit`.

`revisit` means solved with help or shakily — bring it back in a later
session's recall phase.
